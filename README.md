# HarmonyScripts
# Render Layers to Image Sequences
 
A Toon Boom Harmony script that renders every drawing layer in a scene to its own
image sequence — one folder per layer — and writes companion files so an After
Effects compositor can rebuild the scene with almost no manual setup.
